eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    healthcheck:
      enabled: true
  server:
    wait-time-in-ms-when-sync-empty: 0
    eviction-interval-timer-in-ms: 15000
spring:
  application:
    name: pts-service-registry-svc

logging:
  file:
    path: data/logs
